{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamiemulcahy/pa-pedia/pkg/models/faction-metadata",
  "$ref": "#/$defs/FactionMetadata",
  "$defs": {
    "FactionMetadata": {
      "properties": {
        "identifier": {
          "type": "string",
          "description": "Unique identifier for the faction (e.g. com.pa.legion-expansion)"
        },
        "displayName": {
          "type": "string",
          "description": "Human-readable name for the faction"
        },
        "version": {
          "type": "string",
          "description": "Semantic version of the faction data (e.g. 1.2.0)"
        },
        "author": {
          "type": "string",
          "description": "Author or organization that created this faction"
        },
        "description": {
          "type": "string",
          "description": "Brief description of the faction"
        },
        "dateCreated": {
          "type": "string",
          "description": "ISO 8601 date when faction was created (YYYY-MM-DD)"
        },
        "build": {
          "type": "string",
          "description": "PA game build number this faction targets"
        },
        "type": {
          "type": "string",
          "enum": [
            "base-game",
            "mod"
          ],
          "description": "Type of faction (base-game or mod)"
        },
        "mods": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "List of mod identifiers that compose this faction"
        },
        "backgroundImage": {
          "type": "string",
          "description": "Path to faction background image relative to faction folder root"
        },
        "isAddon": {
          "type": "boolean",
          "description": "True if this is an addon mod that extends existing factions rather than defining a new one"
        },
        "baseFactions": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Faction display names that this addon extends (e.g. MLA or Legion)"
        },
        "teamColors": {
          "$ref": "#/$defs/TeamColors",
          "description": "Default faction team-paint colour pair (primary/secondary hex) for the 3D model viewer"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "identifier",
        "displayName",
        "version",
        "type"
      ]
    },
    "TeamColors": {
      "properties": {
        "primary": {
          "type": "string",
          "description": "Default main/primary team colour as a hex string (e.g. #007cff)"
        },
        "secondary": {
          "type": "string",
          "description": "Default highlight/secondary team colour as a hex string (e.g. #ff6400)"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "primary",
        "secondary"
      ]
    }
  },
  "title": "faction-metadata"
}